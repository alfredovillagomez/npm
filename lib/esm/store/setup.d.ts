import { History } from 'history';
import { ApplicationStore } from './utils';
declare const _default: (history: History, reducers: any[], epics: any[], initialState?: {} | undefined) => ApplicationStore;
export default _default;
